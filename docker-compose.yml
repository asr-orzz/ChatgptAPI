services:
  chatgpt-api:
    build: .
    container_name: chatgpt-api
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./sessions:/app/sessions
      - ./debug:/app/debug
